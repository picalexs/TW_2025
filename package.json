{
  "name": "tw_2025",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node backend/server.js",
    "dev": "node scripts/dev.js",
    "dev:server": "set NODE_ENV=development&& node backend/server.js",
    "build": "node scripts/build.js",
    "build:css": "node scripts/build.js css",
    "build:js": "node scripts/build.js js",
    "build:production": "set NODE_ENV=production&& node scripts/build.js production",
    "clean": "node scripts/build.js clean",
    "clean:minified": "powershell -Command \"Get-ChildItem -Path 'frontend' -Recurse -Filter '*.min.*' | Remove-Item -Force\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^6.0.0",
    "compression": "^1.7.4",
    "dotenv": "^16.5.0",
    "jsonwebtoken": "^9.0.2",
    "nodemailer": "^7.0.3",
    "oracledb": "^6.8.0"
  },
  "devDependencies": {
    "chokidar": "^3.5.3",
    "clean-css-cli": "^5.6.3",
    "terser": "^5.29.2"
  }
}
